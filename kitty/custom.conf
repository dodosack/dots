#echo "fastfetch --logo tux" >> ~/.bashrc

background_opacity 0.3
#symbol_map U+30A0-U+30FF "Noto Sans CJK JP"

# Kitty SSH-Integration für korrekte Terminal-Funktionen
alias ssh="kitty +kitten ssh"
